const Comment = {
    // author: (parent, args, {db}, info) => db.users.find(u => u.id === parent.author),
    // post: (parent, args, {db}, info) => db.posts.find(p => p.id === parent.post)
}

export {Comment as default}
